name: CI/CD Pipeline

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]

jobs:
  # CI Job - Testing and validation
  test:
    runs-on: ubuntu-latest
    name: Test and Lint

    strategy:
      matrix:
        node-version: [22.x]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      # TODO: Re-enable tests and validation
      # - name: Run linting
      #   run: npm run lint

      # - name: Run type checking
      #   run: npm run build

      # - name: Run unit tests
      #   run: npm run test

      # - name: Run e2e tests
      #   run: npm run test:e2e
      #   continue-on-error: true # Allow e2e tests to fail without blocking deployment

  # CD Job - Deploy to dev (only on dev branch)
  deploy:
    runs-on: ubuntu-latest
    name: Deploy to VPS
    needs: test
    if: github.ref == 'refs/heads/dev' && github.event_name == 'push'

    steps:
      - name: Deploy to VPS
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            cd ~/app
            echo "🔄 Fetch All..."
            git fetch --all

            echo "🔄 Reset origin/dev..."
            git reset --hard origin/dev
            
            echo "🔄 Pulling latest changes..."
            git pull origin dev
            
            echo "📦 Stopping current services..."
            make down-dev
            
            echo "Start services..."
            make build

            echo "🚀 Starting services..."
            make up-dev-d
            
            echo "🔄 Running database migrations..."
            make migration-run
            
            echo "✅ Deployment completed successfully!"
            
            # Health check
            echo "🏥 Performing health check..."
            sleep 10
            curl -f http://localhost:3000/health || echo "⚠️ Health check failed"
